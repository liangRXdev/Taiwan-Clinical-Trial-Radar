# 下載的來源資料（42 MB ZIP / 166 MB CSV），不進版控
*.zip
205_*.csv

# **但 B 群的注入 fixture 必須進版控**（共 16 KB）。它們被上面的 `*.zip` 掃到，
# 而 clean checkout 上沒有它們時 B1 的整組測試會以 FileNotFoundError 掛掉——
# 本機因為留著產生物而看不到，2026-09-22 的第一次 CI 才現形。
!tests/fixtures/b_failures/inputs/*.zip

# 產生物
# **`public/data/` 不在此列**：§9.2.3 的部署鏈以 `main` 的 commit 為 build 的唯一輸入，
# 發布物必須進版控，否則 Cloudflare Pages 建置時根本沒有資料可送。
# `qa/` 仍不進版控——它是每次執行的診斷輸出，以 workflow artifact 保存。
qa/
node_modules/
__pycache__/
.venv/
dist/

# 連網實跑的暫存（下載的 CSV／ZIP、量測用 staging 與 QA report）
.cache/

# 暫存輸出
tests/fixtures/_*.txt
.venv/
.pytest_cache/
.tmp-verify/
.staging/
reports/
test-results/
playwright-report/

# Remember plugin 的 session 紀錄（本機用，不進版控）
.remember/
